apiVersion: v1
kind: Pod
metadata:
  name: postgres-pod
  labels:
    name: postgres-pod
    app: demo-voting-app
spec:
  containers:
  - name: postgres-pod
    image: postgres:12.2
    ports:
      - containerPort: 5432
    env:
      - name: POSTGRES_PASSWORD
        value: mysecretpassword
